spring:
  application:
    name: ConfigurationService
  cloud:
    consul:
      discovery:
        prefer-ip-address: true
      host: localhost
      #host: ubuntu-vm-1
      port: 8500
  profiles:
    active: native
security:
  basic:
    enable: false
